checkout:
  post:
    - mv heroku.gitignore .gitignore
dependencies:
  post:
    - elm package install --yes
    - npm run build
deployment:
  production:
    branch: master
    heroku:
      appname: l-o-l
