checkout:
  post:
    - mv heroku.gitignore .gitignore
    - git commit -am 'deploy' --author="RGBboy <l-_-l@rgbboy.com>"
dependencies:
  post:
    - elm package install --yes
    - npm run build
deployment:
  production:
    branch: master
    heroku:
      appname: l-o-l
